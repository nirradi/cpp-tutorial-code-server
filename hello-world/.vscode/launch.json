{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug hello (LLDB)",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/hello",
            "args": [],
            "cwd": "${workspaceFolder}",
            "stopOnEntry": false
        }
    ]
}